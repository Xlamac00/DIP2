
<div class="col-md-3 p-0 mb-3" id="{{ section }}{{ role.board.pageId }}">
    <div class="card">
        <div class="card-header rounded-0 pb-3" style="background-color: #ddd">
            <h5>
                <a href="./{{ role.board.url }}" class="text-dark">{{ role.board.name }}</a>
            </h5>
            <p class="text-secondary m-0">{{ role.board.archivedTime }}</p>
        </div>
    </div>
</div>